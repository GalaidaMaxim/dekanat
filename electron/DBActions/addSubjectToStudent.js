const { Students, Subjects } = require("../models");
module.exports = async (idStudent, idSubject) => {
  try {
    const student = await St;
  } catch (err) {
    console.log(err);
    return null;
  }
};
