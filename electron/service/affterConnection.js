const { State, EducationPlan, Subjects, Students } = require("../models");

module.exports = async () => {
  // const students = await Students.find({ course: 1, status: "навчається" });
  // for (let i = 0; i < students.length; i++) {
  //   students[i].startYear = 2025;
  //   await students[i].save();
  // }
};
